# Figure 2.9 build note

Generated by `scripts/build_speckle_crops_figure.py`. Replaces the earlier version at
`reports/model_comparison/model_speckle_crops.png`, which was drawn from the 154-location
model_comparison snapshot with random dedup.

## Data basis

- Classified rasters: the current temporally-matched per-bracket predictions
  `data/raw/transfer_predictions/<bracket>/pred_<bracket>_cell<id>.tif`, bands 1 to 5 = v2 to v6, over
  the 180 adjudicated cells. These are the same classifications behind the current spatial-structure
  diagnostics (Table 2.6, spatial_structure/with_spec_all/), not the model_comparison snapshot.
- Palette: the standard 10-class palette from `compare_interpreted_vs_model.load_mappings`, matching
  the other Chapter 2 maps.

## Crop location

Cell 31320 (bracket 2018_2020) was used. Its footprint (464760, 2593250, 468130, 2596620 in EPSG:5070)
equals the earlier crop bounds exactly, so the location is reused for continuity. It contains a water
body that is contiguous under v2 to v5 and dissolves into speckle under v6.

## Neighbor-change values (current, over all 180 cells)

| config | current (annotated) | earlier snapshot |
|--------|---------------------|------------------|
| v2 | 0.085 | 0.074 |
| v3 | 0.084 | 0.077 |
| v4 | 0.155 | 0.135 |
| v5 | 0.093 | 0.091 |
| v6 | 0.801 | 0.781 |

The annotated values are the current ones, computed over the 180 per-bracket cells. They run modestly
higher than the earlier snapshot values (which came from the full static mosaic over about 2.4 billion
pixel pairs), but the ordering and the smooth versus speckly character are preserved: v2, v3, and v5
near 0.08 to 0.09, v4 higher at 0.155, and v6 dominant at 0.801. The difference is expected, since the
current per-bracket classifications and the earlier static-mosaic classifications are different maps on
different footprints.

## Consistency notes

- This figure no longer draws from the 154-location snapshot, so consistency_report flag on the
  superseded arm no longer applies to Figure 2.9.
- Table S2 (dedup-selection sensitivity) still references the earlier arm; it is left as-is.
- Table S3 (map speckle) was regenerated on the current 180-cell basis
  (reports/model_comparison_current/model_speckle.csv) and now matches Figure 2.9 (v2 0.085, v3 0.084,
  v4 0.155, v5 0.093, v6 0.801).

## Output

- `manuscript_formatting/figures/figure_2_9_speckle_crops.png` at 300 dpi.
- `manuscript_formatting/figures/figure_2_9_speckle_crops.pdf` (vector).
